FROM continuumio/miniconda3:latest as build

RUN conda install -y -c conda-forge conda-pack conda-libmamba-solver && \
    # conda install pip pkgconfig conda-forge::hdf5 && \
    pip install kipoi && \
    kipoi env create Framepool --source=kipoi 
    # && \
    # conda clean -afy

WORKDIR /kipoi-Framepool
RUN conda-pack -n kipoi-Framepool -o /tmp/kipoi-Framepool.tar --ignore-missing-files && \
    tar xf /tmp/kipoi-Framepool.tar && \
    rm /tmp/kipoi-Framepool.tar

FROM debian:bullseye-slim   

RUN apt-get update && apt-get install --no-install-recommends -y ca-certificates git \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

RUN mkdir -p /app
COPY --from=build /kipoi-Framepool /opt/conda/envs/kipoi-Framepool

SHELL ["/bin/bash", "-c"]
RUN echo "source /opt/conda/envs/kipoi-Framepool/bin/activate" > ~/.bashrc
ENV PATH /opt/conda/envs/kipoi-Framepool/bin:$PATH

SHELL ["conda", "run", "-n", "kipoi-Framepool", "/bin/bash", "-c"]


